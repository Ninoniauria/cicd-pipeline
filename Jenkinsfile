pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        script {
          print ('hello world')
        }

      }
    }

  }
  environment {
    registry = 'ninoniauri/task'
  }
}